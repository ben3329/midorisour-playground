---
title: Resume
description: One-page resume
---

import Heading from '@theme/Heading';

export const skills = [
  { label: 'Backend', items: ['Python', 'FastAPI', 'Flask', 'SQLModel', 'Celery'] },
  { label: 'Cloud / DevOps', items: ['AWS', 'Kubernetes', 'Docker', 'GitHub Actions', 'GitLab CI/CD'] },
  { label: 'Frontend / Mobile', items: ['Flutter'] },
  { label: 'Others', items: ['C', 'C++'] },
];

export const links = [
  { label: 'Email', value: 'ben3329@naver.com', href: 'mailto:ben3329@naver.com' },
  { label: 'GitHub', value: 'github.com/ben3329', href: 'https://github.com/ben3329' },
  { label: 'LinkedIn', value: 'linkedin.com/in/weonhyeok-ji-8810ba168', href: 'https://www.linkedin.com/in/weonhyeok-ji-8810ba168' },
];

export const experiences = [
  {
    company: '피노키오랩주식회사',
    role: '백엔드 개발자 · 팀 리드',
    period: '2023.09 ~ 현재',
    projects: [
      {
        name: '침해 검사 시스템 MSA 전환',
        period: '2025.03 ~ 2025.05',
        summary: '레거시 CDS를 서비스 경계를 다시 정의한 MSA 구조로 재설계해 초기 사업 요구 변화에도 대응 가능한 플랫폼을 구축했습니다.',
        highlights: [
          '실사용자 주간 미팅으로 요구사항을 MVP 우선순위로 재조정하고 핵심 기능을 2개월 내 베타 배포',
          'Docker Compose + Traefik 기반 로컬 통합 실행 환경을 설계해 서비스 구동 시간을 80% 이상 단축',
          '모노레포 + Poetry 패키징 전략을 도입해 서비스 간 공통 모듈을 공유하고 의존성 충돌을 제거',
        ],
        stack: ['FastAPI', 'Poetry', 'Docker', 'Traefik', 'MySQL', 'AWS'],
      },
      {
        name: 'TIPA 업무 시스템 리팩토링',
        period: '2023.09 ~ 2024.06',
        summary: 'TIPA/TIMS 등 레거시 백엔드를 최신 스택으로 교체하고 운영 중 발생하던 데이터 무결성 문제를 해결했습니다.',
        highlights: [
          '로그 구조를 단일 표준 테이블로 재정의하고 중앙화하여 이슈 추적 시간을 단축',
          'Alembic을 도입해 ORM/DB 간 스키마 괴리를 제거하고 배포 파이프라인에 마이그레이션을 편입',
          'Foreign Key 및 영문 스키마 재설계로 권한/다국어 요구사항을 안정적으로 수용',
        ],
        stack: ['Flask', 'FastAPI', 'SQLAlchemy', 'Alembic', 'MySQL'],
      },
      {
        name: 'AI 모델 서빙 및 정확도 개선',
        period: '2024.04 ~ 2025.03',
        summary: 'AI 모델이 탑재된 침해 검사/정품 인증 서비스를 위해 서빙 인프라와 모델 품질 개선을 동시에 수행했습니다.',
        highlights: [
          'Kubernetes + Celery + Redis 조합으로 비동기 요청 파이프라인을 구성하고 CronJob으로 결과를 DB에 자동 적재',
          '거짓 음성 사례를 DB화해 다중 점수 기반 판별 로직을 만들고, OpenCV 마스킹으로 거짓 양성을 감소',
          'Flutter 앱에 Kotlin/Swift 네이티브 모듈을 붙여 실시간 흐림도 검사를 제공, 촬영 UX와 서버 비용을 동시에 개선',
        ],
        stack: ['FastAPI', 'SQLModel', 'Celery', 'Kubernetes', 'Redis', 'OpenCV', 'MySQL', 'AWS'],
      },
      {
        name: '사내 공용 라이브러리 · 업무 프로세스 정착',
        period: '2024.01 ~ 2025.04',
        summary: '팀 운영 전반을 표준화하고 백엔드 공용 라이브러리를 제작해 신규 프로젝트 착수 시간을 줄였습니다.',
        highlights: [
          'MS Teams Planner/Approvals 기반 칸반과 전자결재, 코드 리뷰, 기술 세미나 프로세스를 정립',
          'Wiki.js로 업무/연구 결과를 문서화해 지식 자산을 지속적으로 축적',
          'DB 세션, AWS S3 업로드, Cognito 인증 등을 묶은 Python 라이브러리를 만들어 Nexus에 배포하여 공통 기능을 통일',
        ],
        stack: ['Python', 'MS Teams', 'Notion', 'Wiki.js', 'Nexus Repository'],
      },
    ],
  },
  {
    company: '주식회사 윈스',
    role: '주임 연구원',
    period: '2019.11 ~ 2023.03',
    projects: [
      {
        name: 'IPS 클라우드 제품화',
        period: '2022.01 ~ 2023.02',
        summary: 'IPS 솔루션을 클라우드 환경으로 전환하기 위한 테스트베드와 네트워크 설정 기능을 개발했습니다.',
        highlights: [
          'OpenStack 기반 사내 프라이빗 클라우드를 구축하고 Open vSwitch·Kubernetes로 네트워크 가상화를 구현',
          'C/Bash/Python으로 인터페이스·라우팅 설정 기능 개발',
          'GitLab 기반 CI/CD 파이프라인을 통한 빌드/배포 자동화',
        ],
        stack: ['C', 'Bash','Python', 'OpenStack', 'Open vSwitch', 'Kubernetes', 'GitLab CI'],
      },
      {
        name: 'API 테스트 자동화',
        period: '2021.06 ~ 2021.12',
        summary: 'Robot Framework로 서버 API 전수 테스트를 자동화해 QA 리소스를 대폭 줄였습니다.',
        highlights: [
          '주요 기능을 시나리오화하여 주말 야간 자동 실행 체계를 마련, 릴리즈 준비 기간을 이틀 이내로 단축(기존 4명/1주일 -> 1명/2일)',
          '동시성 테스트 등 수동으로 처리하기 어려운 케이스까지 자동화해 테스트 범위를 확대',
        ],
        stack: ['Python', 'Robot Framework'],
      },
      {
        name: 'NGFW 성능 개선',
        period: '2021.06 ~ 2022.01',
        summary: '차세대 방화벽의 성능 병목 구간을 분석하고 최적화하여 처리량과 안정성을 향상시켰습니다.',
        highlights: [
          '기존 탐지 정책 분석 모듈의 성능 병목 구간을 프로파일링 도구로 식별 및 개선',
          'ANTLR4 + C++로 PCRE 패턴 파서를 Python C Extension 모듈로 구현해 Python에서 복잡한 탐지 정책을 고속으로 분석',
        ],
        stack: ['C++', 'Python', 'ANTLR4', 'cProfile', 'pstats'],
      },
      {
        name: 'KISA/ETRI 국책 과제 연구',
        period: '2020.04 ~ 2022.06',
        summary: '5G 및 HTTP/2 보안 기술 연구를 수행하고, Free5GC 기반 가상 5G 망과 HTTP/2 보안 실험 환경을 구축했습니다.',
        highlights: [
          '5G, HTTP2 기술 자료 조사 및 보안 적용 방안 연구',
          'Free5GC 기반 가상 5G 망과 HTTP/2 보안 실험 환경을 구성',
          '패킷 분석을 위해 Free5GC 소스 코드를 분석하여 TLS Cipher Suite를 변경',
        ],
        stack: ['Go', 'Free5GC', 'Kubernetes', 'Docker'],
      },
    ],
  },
];

export const education = [
  { school: '학점은행제', detail: '정보보호학 (학사)', period: '2017.03 ~ 2019.02' },
  { school: '협성대학교', detail: '신학 (학사)', period: '2010.03 ~ 2014.02' },
];

export const certifications = [{ name: '정보처리기사', year: '2018.05' }];

export const Pill = ({ children }) => <span className="resume-pill">{children}</span>;

export const Headline = () => (
  <div className="resume-hero">
    <div>
      <p className="resume-eyebrow">Backend · DevOps · Team Lead</p>
      <Heading as="h1">
        지원혁 <span className="resume-hero__role">Backend Engineer</span>
      </Heading>
      <p className="resume-hero__lead">
        Python/FastAPI로 서비스를 설계하며 운영까지 책임져 온 백엔드 개발자입니다.
        요구사항이 자주 바뀌는 환경에서도 구조를 다듬고 DevOps, 기획, 협업 프로세스까지 직접 구축해 제품 개선 속도를 높였습니다.
      </p>
    </div>
    <div className="resume-hero__pills">
      <Pill>Python &amp; FastAPI</Pill>
      <Pill>DevOps &amp; Architecture</Pill>
      <Pill>Team Leading</Pill>
    </div>
  </div>
);

<div className="resume-page">
  <Headline />

  <div className="resume-grid">
    <section className="resume-card">
      <p className="resume-eyebrow">Profile</p>
      <h2>문제를 끝까지 파고드는 백엔드 개발자</h2>
      <p>
        안녕하세요. 백엔드 개발자 지원혁입니다.
      </p>
      <p>
        Python과 FastAPI를 중심으로 다양한 라이브러리와 아키텍처를 다루며,
        복잡한 문제를 끝까지 추적해 해결하면서 경험을 쌓았습니다.
      </p>
      <p>
        서비스 기획·설계부터 DevOps와 팀 리딩까지 직접 맡으며 협업 구조와 운영 프로세스를 개선했습니다.
      </p>
    </section>

    <section className="resume-card">
      <p className="resume-eyebrow">Skills</p>
      <h2>핵심 역량</h2>
      <div className="resume-skill-grid">
        {skills.map(({ label, items }) => (
          <div key={label} className="resume-skill">
            <p>{label}</p>
            <div className="resume-tag-list">
              {items.map((item) => (
                <span key={item} className="resume-tag">
                  {item}
                </span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    <section className="resume-card">
      <p className="resume-eyebrow">Links</p>
      <h2>연락처 &amp; 링크</h2>
      <ul className="resume-link-list">
        {links.map((link) => (
          <li key={link.label}>
            <span>{link.label}</span>
            <a href={link.href} target="_blank" rel="noreferrer">
              {link.value}
            </a>
          </li>
        ))}
      </ul>
    </section>
  </div>

  <section className="resume-section resume-experience">
    <div className="resume-section__header">
      <p className="resume-eyebrow">Experience</p>
      <h2>경력 요약</h2>
      <p>서비스 설계부터 DevOps와 팀 프로세스까지 제품 전 과정을 주도하며 실행력을 높였습니다.</p>
    </div>

    <div className="resume-timeline">
      {experiences.map((exp) => {
        const hasProjectDetails = Array.isArray(exp.projects) && exp.projects.length > 0;

        return (
          <article key={exp.company} className="resume-timeline__item">
            <div className="resume-timeline__meta">
              <p className="resume-eyebrow">{exp.period}</p>
              <h3>{exp.company}</h3>
              <p className="resume-role">{exp.role}</p>
              {!hasProjectDetails && exp.project && (
                <p className="resume-project">{exp.project}</p>
              )}
            </div>

            {hasProjectDetails ? (
              <div className="resume-project-grid">
                {exp.projects.map((project, index) => {
                  const highlightList = Array.isArray(project.highlights) ? project.highlights : [];
                  const stackList = Array.isArray(project.stack) ? project.stack : [];
                  const key = `${exp.company}-project-${project.name ?? index}`;

                  return (
                    <div key={key} className="resume-project-card">
                      <div className="resume-project-card__header">
                        <p className="resume-project-card__name">{project.name}</p>
                        {project.period && (
                          <span className="resume-project-card__period">{project.period}</span>
                        )}
                      </div>
                      {project.summary && <p>{project.summary}</p>}
                      {highlightList.length > 0 && (
                        <ul>
                          {highlightList.map((highlight) => (
                            <li key={`${key}-${highlight}`}>{highlight}</li>
                          ))}
                        </ul>
                      )}
                      {stackList.length > 0 && (
                        <div className="resume-tag-list">
                          {stackList.map((tech) => (
                            <span
                              key={`${key}-${tech}`}
                              className="resume-tag resume-tag--ghost"
                            >
                              {tech}
                            </span>
                          ))}
                        </div>
                      )}
                    </div>
                  );
                })}
              </div>
            ) : (
              <>
                <p>{exp.summary}</p>
                <ul>
                  {exp.highlights.map((highlight) => (
                    <li key={highlight}>{highlight}</li>
                  ))}
                </ul>
                <div className="resume-tag-list">
                  {exp.stack.map((tech) => (
                    <span key={tech} className="resume-tag resume-tag--ghost">
                      {tech}
                    </span>
                  ))}
                </div>
              </>
            )}
          </article>
        );
      })}
    </div>
  </section>

  <div className="resume-grid">
    <section className="resume-card">
      <p className="resume-eyebrow">Education</p>
      <h2>학력</h2>
      <ul className="resume-list">
        {education.map((item) => (
          <li key={item.school}>
            <div>
              <strong>{item.school}</strong>
              <p>{item.detail}</p>
            </div>
            <span>{item.period}</span>
          </li>
        ))}
      </ul>
    </section>

    <section className="resume-card">
      <p className="resume-eyebrow">Certifications</p>
      <h2>자격증</h2>
      <ul className="resume-list">
        {certifications.map((cert) => (
          <li key={cert.name}>
            <div>
              <strong>{cert.name}</strong>
            </div>
            <span>{cert.year}</span>
          </li>
        ))}
      </ul>
    </section>
  </div>
</div>

<style>{`
:root {
  --resume-accent: #3fce7b;
  --resume-muted: #4b5b55;
  --resume-dark: #083127;
  --resume-card-bg: rgba(246, 255, 249, 0.92);
  --resume-border: rgba(63, 206, 123, 0.35);
  --resume-card-border: rgba(8, 49, 39, 0.08);
  --resume-card-shadow: 0 15px 32px -22px rgba(8, 49, 39, 0.45);
  --resume-hero-bg: radial-gradient(circle at top, rgba(63, 206, 123, 0.22), transparent 55%),
    linear-gradient(135deg, rgba(10, 138, 118, 0.25), rgba(63, 206, 123, 0.45));
  --resume-hero-shadow: 0 25px 50px -18px rgba(8, 49, 39, 0.35);
  --resume-pill-bg: rgba(63, 206, 123, 0.18);
  --resume-pill-border: rgba(8, 49, 39, 0.12);
  --resume-tag-bg: rgba(63, 206, 123, 0.18);
  --resume-tag-ghost-bg: rgba(8, 49, 39, 0.05);
  --resume-tag-ghost-border: rgba(8, 49, 39, 0.25);
  --resume-divider: rgba(8, 49, 39, 0.12);
  --resume-marker-glow: rgba(63, 206, 123, 0.25);
}

[data-theme='dark'] {
  --resume-muted: #9dcfb5;
  --resume-dark: #f4fff9;
  --resume-card-bg: rgba(5, 21, 18, 0.92);
  --resume-border: rgba(63, 206, 123, 0.55);
  --resume-card-border: rgba(63, 206, 123, 0.25);
  --resume-card-shadow: 0 20px 40px -22px rgba(0, 0, 0, 0.65);
  --resume-hero-bg: radial-gradient(circle at top, rgba(92, 255, 190, 0.15), transparent 55%),
    linear-gradient(135deg, rgba(5, 76, 63, 0.85), rgba(21, 131, 109, 0.65));
  --resume-hero-shadow: 0 30px 60px -25px rgba(0, 0, 0, 0.75);
  --resume-pill-bg: rgba(63, 206, 123, 0.22);
  --resume-pill-border: rgba(63, 206, 123, 0.4);
  --resume-tag-bg: rgba(63, 206, 123, 0.2);
  --resume-tag-ghost-bg: rgba(255, 255, 255, 0.06);
  --resume-tag-ghost-border: rgba(159, 255, 210, 0.3);
  --resume-divider: rgba(255, 255, 255, 0.08);
  --resume-marker-glow: rgba(63, 206, 123, 0.35);
}

.resume-page {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
  padding-bottom: 4rem;
}

.resume-hero {
  background: var(--resume-hero-bg);
  border: 1px solid var(--resume-border);
  border-radius: 1.5rem;
  padding: 2.75rem;
  display: flex;
  flex-wrap: wrap;
  gap: 2rem;
  align-items: center;
  box-shadow: var(--resume-hero-shadow);
}

.resume-hero__role {
  font-size: 1.5rem;
  font-weight: 500;
  color: var(--resume-muted);
  margin-left: 0.75rem;
}

.resume-hero__lead {
  margin-top: 1rem;
  font-size: 1.05rem;
  line-height: 1.8;
  max-width: 60ch;
}

.resume-hero__pills {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
}

.resume-pill {
  background: var(--resume-pill-bg);
  color: var(--resume-dark);
  border-radius: 999px;
  padding: 0.4rem 1rem;
  font-size: 0.95rem;
  border: 1px solid var(--resume-pill-border);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.resume-eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.12em;
  font-size: 0.8rem;
  color: var(--resume-muted);
  margin-bottom: 0.6rem;
}

.resume-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 1.5rem;
}

.resume-card,
.resume-section {
  background: var(--resume-card-bg);
  border-radius: 1.25rem;
  padding: 1.75rem;
  border: 1px solid var(--resume-card-border);
  box-shadow: var(--resume-card-shadow);
  backdrop-filter: blur(12px);
}

.resume-card h2,
.resume-section h2 {
  margin-top: 0;
  margin-bottom: 0.75rem;
}

.resume-skill-grid {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.resume-tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
}

.resume-tag {
  background: var(--resume-tag-bg);
  color: var(--resume-dark);
  border-radius: 0.7rem;
  padding: 0.2rem 0.75rem;
  font-size: 0.85rem;
}

.resume-tag--ghost {
  background: var(--resume-tag-ghost-bg);
  border: 1px dashed var(--resume-tag-ghost-border);
}

.resume-link-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 0.85rem;
}

.resume-link-list li {
  display: flex;
  flex-direction: column;
  gap: 0.1rem;
}

.resume-link-list span {
  font-size: 0.85rem;
  color: var(--resume-muted);
}

.resume-link-list a {
  color: var(--resume-dark);
  font-weight: 600;
  text-decoration: none;
}

.resume-section__header {
  margin-bottom: 1.75rem;
}

.resume-timeline {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.resume-timeline__item {
  border-left: 2px solid var(--resume-border);
  padding-left: 1.5rem;
  position: relative;
}

.resume-timeline__item::before {
  content: '';
  width: 12px;
  height: 12px;
  border-radius: 999px;
  background: var(--resume-accent);
  position: absolute;
  left: -7px;
  top: 0.4rem;
  box-shadow: 0 0 0 6px var(--resume-marker-glow);
}

.resume-role {
  font-weight: 600;
}

.resume-project {
  font-size: 0.9rem;
  color: var(--resume-muted);
}

.resume-project-grid {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
  margin-top: 1rem;
}

.resume-project-card {
  border: 1px dashed var(--resume-border);
  border-radius: 0.9rem;
  padding: 1rem 1.25rem;
  background: var(--resume-card-bg);
  box-shadow: inset 0 0 0 1px rgba(8, 49, 39, 0.02);
}

.resume-project-card ul {
  margin: 0.5rem 0 0.75rem;
  padding-left: 1.1rem;
}

.resume-project-card ul li {
  margin-bottom: 0.25rem;
}

.resume-project-card ul li:last-child {
  margin-bottom: 0;
}

.resume-project-card__header {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  align-items: baseline;
  margin-bottom: 0.45rem;
}

.resume-project-card__name {
  font-weight: 600;
}

.resume-project-card__period {
  font-size: 0.85rem;
  color: var(--resume-muted);
}

.resume-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.resume-list li {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  border-bottom: 1px dashed var(--resume-divider);
  padding-bottom: 0.75rem;
}

.resume-list li:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

@media (max-width: 768px) {
  .resume-hero {
    padding: 1.75rem;
  }

  .resume-hero__role {
    display: block;
    margin-left: 0;
    margin-top: 0.25rem;
  }

  .resume-timeline__item {
    padding-left: 1rem;
  }
}
`}</style>
