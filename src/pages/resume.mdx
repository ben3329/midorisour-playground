---
title: Resume
description: One-page resume
---

import Heading from "@theme/Heading";

export const skills = [
  {
    label: "Backend",
    items: ["Python", "FastAPI", "Flask", "SQLModel", "Celery"],
  },
  {
    label: "Cloud / DevOps",
    items: ["AWS", "Kubernetes", "Docker", "GitHub Actions", "GitLab CI/CD"],
  },
  { label: "Frontend / Mobile", items: ["Flutter"] },
  { label: "Others", items: ["C", "C++"] },
];

export const links = [
  {
    label: "Email",
    value: "ben3329@naver.com",
    href: "mailto:ben3329@naver.com",
  },
  {
    label: "GitHub",
    value: "github.com/ben3329",
    href: "https://github.com/ben3329",
  },
  {
    label: "LinkedIn",
    value: "linkedin.com/in/weonhyeok-ji-8810ba168",
    href: "https://www.linkedin.com/in/weonhyeok-ji-8810ba168",
  },
];

export const experiences = [
  {
    company: "피노키오랩주식회사",
    role: "백엔드 개발자 · 팀 리드",
    period: "2023.09 ~ 현재",
    projects: [
      // {
      //   name: "title",
      //   period: "2025.02 ~ 현재",
      //   summary:
      //     "summary",
      //   highlights: [
      //     "highlight",
      //   ],
      //   stack: [
      //     "tech",
      //   ],
      // },
      {
        name: "통관 단계 지재권 감정 지원 시스템 리뉴얼",
        period: "2025.02 ~ 현재",
        summary:
          "운영 중인 통관 단계 지재권 감정 시스템을 리뉴얼하면서 외부 연계·보안·성능을 중심으로 백엔드 아키텍처를 재설계하고, 고객사 전용 조회/API 시스템까지 확장했습니다.",
        highlights: [
          "고객사 전용 외부 데이터 조회 시스템을 별도 서비스로 분리 설계해 내부 TIMS와 격리하고, 계정/권한·증명서 발급·공개 API를 구축",
          "AWS Cognito 기반 인증/인가와 SlowAPI + ElastiCache 기반 rate limiting·cache를 도입해 외부 접근 안정성과 운영 신뢰성 강화",
          "복합 조회 View에 대해 Index 재설계, Computed Column, JOIN 전략 최적화를 적용해 응답 성능을 약 3초에서 0.1초 이하로 개선",
          "개인정보 비저장 요구가 있는 의료 지표 프로젝트에서 AWS Textract 마스킹 및 오버레이 이미지 생성 파이프라인(OpenCV/Pillow) 구현",
          "FastAPI OpenAPI 문서 체계를 고도화하고 코드 생성기 호환 이슈(FilterDepends override)를 해결해 프론트엔드 연동 생산성 향상",
        ],
        stack: [
          "FastAPI",
        ],
      },
      {
        name: "백엔드 공용 라이브러리 개발",
        period: "2024.12 ~ 2024.12",
        summary:
          "사내 서비스 전반에서 중복되던 핵심 기능들을 모듈화해 Python 공용 라이브러리로 통합하고, 배포·사용 체계를 표준화했습니다.",
        highlights: [
          "DB 세션 관리, 이메일 전송, AWS S3 업로드, Cognito 인증 로직 등 공통 기능을 Python 기반 모듈로 통합",
          "사내 Nexus Repository에 패키지 배포 파이프라인 구축",
          "사내 공용 라이브러리로 도입해 코드 표준화 및 개발·유지보수 비용 절감",
        ],
        stack: ["FastAPI", "SQLModel", "Nexus Repository"],
      },
      {
        name: "통관 단계 지재권 감정 지원 시스템 개발 및 유지보수",
        period: "2023.09 ~ 현재",
        summary:
          "기존 시스템을 FastAPI 기반으로 재구축하고, 비정규화된 DB 구조를 재설계하여 시스템의 안정성과 확장성을 크게 개선했습니다.",
        highlights: [
          "Alembic 기반 DB 마이그레이션 도입",
          "기존 Flask 기반 시스템을 FastAPI 기반으로 리팩토링",
          "정규화 되지않은 DB 스키마와 분산된 로그 구조를 정규화·중앙화",
          "권한 관리 기획 및 개발",
          "Flutter로 기본 감정서 생성, 직인 관리 페이지 등 일부 페이지 개발",
        ],
        stack: [
          "FastAPI",
          "SQLModel",
          "MySQL",
          "Alembic",
          "AWS",
          "Flutter",
          "Flask",
        ],
      },
      {
        name: "업무 프로세스 및 사내 문화 개선",
        period: "2024.01 ~ 현재",
        summary:
          "팀 리드로서 개발 프로세스와 협업 체계를 정립해 조직 생산성과 의사소통 효율을 개선했습니다.",
        highlights: [
          "비체계적으로 병렬 진행되던 개발 환경을 단일 프로젝트 중심·기획 우선 프로세스로 전환",
          "일정관리를 위한 칸반보드 도입",
          "코드 리뷰 도입",
          "주간 업무 회의 및 회고 문화 정착",
          "지식 공유를 위한 사내 위키 구축",
          "휴가 결재 등 전자 결재 시스템 도입",
        ],
        stack: [
          "MS Loop",
          "MS Teams Planner",
          "MS Teams Approvals",
          "Draw.io",
          "Wiki.js",
          "GitHub",
        ],
      },
    ],
  },
  {
    company: "주식회사 윈스",
    role: "주임 연구원",
    period: "2019.11 ~ 2023.03",
    projects: [
      {
        name: "IPS 클라우드 제품화",
        period: "2022.01 ~ 2023.02",
        summary:
          "IPS 솔루션을 클라우드 환경으로 전환하기 위한 테스트베드와 네트워크 설정 기능을 개발했습니다.",
        highlights: [
          "OpenStack 기반 사내 프라이빗 클라우드를 구축하고 Open vSwitch·Kubernetes로 네트워크 가상화를 구현",
          "C/Bash/Python으로 인터페이스·라우팅 설정 기능 개발",
          "GitLab 기반 CI/CD 파이프라인을 통한 빌드/배포 자동화",
        ],
        stack: [
          "C",
          "Bash",
          "Python",
          "OpenStack",
          "Open vSwitch",
          "Kubernetes",
          "GitLab CI",
        ],
      },
      {
        name: "API 테스트 자동화",
        period: "2021.06 ~ 2021.12",
        summary:
          "Robot Framework 기반 API 테스트 자동화를 구축해 기능 테스트·동시성 테스트·회귀 테스트를 자동화하고 QA 투입 리소스를 대폭 절감했습니다.",
        highlights: [
          "주요 기능을 시나리오화하여 주말 야간 자동 실행 체계를 마련, 릴리즈 준비 기간을 이틀 이내로 단축(기존 4명/1주일 -> 1명/2일)",
          "동시성 테스트 등 수동으로 처리하기 어려운 케이스까지 자동화해 테스트 범위를 확대",
        ],
        stack: ["Python", "Robot Framework"],
      },
      {
        name: "NGFW 성능 개선",
        period: "2021.06 ~ 2022.01",
        summary:
          "차세대 방화벽의 정책 분석 모듈의 병목을 프로파일링하여 성능을 최적화하고, 고성능 패턴 분석을 위해 C++ 기반 파서 모듈을 Python C Extension 형태로 구현했습니다.",
        highlights: [
          "cProfile/pstats로 정책 분석 로직의 병목 탐지 및 최적화",
          "ANTLR4 + C++로 PCRE 패턴 파서를 Python C Extension 모듈로 구현해 Python에서 복잡한 탐지 정책을 고속으로 분석",
        ],
        stack: ["C++", "Python", "ANTLR4", "cProfile", "pstats"],
      },
      {
        name: "KISA/ETRI 국책 과제 연구",
        period: "2020.04 ~ 2022.06",
        summary:
          "5G·HTTP/2 보안 기술 연구를 수행하고 Free5GC 기반 가상 5G 망과 HTTP/2 보안 실험 환경을 구축했으며, 패킷 분석을 위해 Free5GC 내부 TLS 설정을 수정했습니다.",
        highlights: [
          "5G, HTTP2 기술 자료 조사 및 보안 적용 방안 연구",
          "Free5GC 기반 가상 5G 망과 HTTP/2 보안 실험 환경을 구성",
          "TLS Cipher Suite 변경을 위해 Free5GC 소스 코드 분석 및 보안 설정 수정",
        ],
        stack: ["Go", "Free5GC", "Kubernetes", "Docker"],
      },
    ],
  },
];

export const education = [
  {
    school: "학점은행제",
    detail: "정보보호학 (학사)",
    period: "2017.03 ~ 2019.02",
  },
  { school: "협성대학교", detail: "신학 (학사)", period: "2010.03 ~ 2014.02" },
];

export const certifications = [{ name: "정보처리기사", year: "2018.05" }];

export const Pill = ({ children }) => (
  <span className="resume-pill">{children}</span>
);

export const Headline = () => (
  <div className="resume-hero">
    <div>
      <p className="resume-eyebrow">Backend · DevOps · Team Lead</p>
      <Heading as="h1">
        지원혁 <span className="resume-hero__role">Backend Engineer</span>
      </Heading>
      <p className="resume-hero__lead">
        Python/FastAPI로 서비스를 설계하며 운영까지 책임져 온 백엔드
        개발자입니다. 요구사항이 자주 바뀌는 환경에서도 구조를 다듬고 DevOps,
        기획, 협업 프로세스까지 직접 구축해 제품 개선 속도를 높였습니다.
      </p>
    </div>
    <div className="resume-hero__pills">
      <Pill>Python &amp; FastAPI</Pill>
      <Pill>DevOps &amp; Architecture</Pill>
      <Pill>Team Leading</Pill>
    </div>
  </div>
);

<div className="resume-page">
  <Headline />

  <div className="resume-grid">
    <section className="resume-card">
      <p className="resume-eyebrow">Profile</p>
      <h2>문제를 끝까지 파고드는 백엔드 개발자</h2>
      <p>
        안녕하세요. 백엔드 개발자 지원혁입니다.
      </p>
      <p>
        Python과 FastAPI를 중심으로 다양한 라이브러리와 아키텍처를 다루며,
        복잡한 문제를 끝까지 추적해 해결하면서 경험을 쌓았습니다.
      </p>
      <p>
        서비스 기획·설계부터 DevOps와 팀 리딩까지 직접 맡으며 협업 구조와 운영 프로세스를 개선했습니다.
      </p>
    </section>

    <section className="resume-card">
      <p className="resume-eyebrow">Skills</p>
      <h2>핵심 역량</h2>
      <div className="resume-skill-grid">
        {skills.map(({ label, items }) => (
          <div key={label} className="resume-skill">
            <p>{label}</p>
            <div className="resume-tag-list">
              {items.map((item) => (
                <span key={item} className="resume-tag">
                  {item}
                </span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    <section className="resume-card">
      <p className="resume-eyebrow">Links</p>
      <h2>연락처 &amp; 링크</h2>
      <ul className="resume-link-list">
        {links.map((link) => (
          <li key={link.label}>
            <span>{link.label}</span>
            <a href={link.href} target="_blank" rel="noreferrer">
              {link.value}
            </a>
          </li>
        ))}
      </ul>
    </section>

  </div>

  <section className="resume-section resume-experience">
    <div className="resume-section__header">
      <p className="resume-eyebrow">Experience</p>
      <h2>경력 요약</h2>
      <p>서비스 설계부터 DevOps와 팀 프로세스까지 제품 전 과정을 주도하며 실행력을 높였습니다.</p>
    </div>

    <div className="resume-timeline">
      {experiences.map((exp) => {
        const hasProjectDetails = Array.isArray(exp.projects) && exp.projects.length > 0;

        return (
          <article key={exp.company} className="resume-timeline__item">
            <div className="resume-timeline__meta">
              <p className="resume-eyebrow">{exp.period}</p>
              <h3>{exp.company}</h3>
              <p className="resume-role">{exp.role}</p>
              {!hasProjectDetails && exp.project && (
                <p className="resume-project">{exp.project}</p>
              )}
            </div>

            {hasProjectDetails ? (
              <div className="resume-project-grid">
                {exp.projects.map((project, index) => {
                  const highlightList = Array.isArray(project.highlights) ? project.highlights : [];
                  const stackList = Array.isArray(project.stack) ? project.stack : [];
                  const key = `${exp.company}-project-${project.name ?? index}`;

                  return (
                    <div key={key} className="resume-project-card">
                      <div className="resume-project-card__header">
                        <p className="resume-project-card__name">{project.name}</p>
                        {project.period && (
                          <span className="resume-project-card__period">{project.period}</span>
                        )}
                      </div>
                      {project.summary && <p>{project.summary}</p>}
                      {highlightList.length > 0 && (
                        <ul>
                          {highlightList.map((highlight) => (
                            <li key={`${key}-${highlight}`}>{highlight}</li>
                          ))}
                        </ul>
                      )}
                      {stackList.length > 0 && (
                        <div className="resume-tag-list">
                          {stackList.map((tech) => (
                            <span
                              key={`${key}-${tech}`}
                              className="resume-tag resume-tag--ghost"
                            >
                              {tech}
                            </span>
                          ))}
                        </div>
                      )}
                    </div>
                  );
                })}
              </div>
            ) : (
              <>
                <p>{exp.summary}</p>
                <ul>
                  {exp.highlights.map((highlight) => (
                    <li key={highlight}>{highlight}</li>
                  ))}
                </ul>
                <div className="resume-tag-list">
                  {exp.stack.map((tech) => (
                    <span key={tech} className="resume-tag resume-tag--ghost">
                      {tech}
                    </span>
                  ))}
                </div>
              </>
            )}
          </article>
        );
      })}
    </div>

  </section>

  <div className="resume-grid">
    <section className="resume-card">
      <p className="resume-eyebrow">Education</p>
      <h2>학력</h2>
      <ul className="resume-list">
        {education.map((item) => (
          <li key={item.school}>
            <div>
              <strong>{item.school}</strong>
              <p>{item.detail}</p>
            </div>
            <span>{item.period}</span>
          </li>
        ))}
      </ul>
    </section>

    <section className="resume-card">
      <p className="resume-eyebrow">Certifications</p>
      <h2>자격증</h2>
      <ul className="resume-list">
        {certifications.map((cert) => (
          <li key={cert.name}>
            <div>
              <strong>{cert.name}</strong>
            </div>
            <span>{cert.year}</span>
          </li>
        ))}
      </ul>
    </section>

  </div>
</div>

<style>{`
:root {
  --resume-accent: #3fce7b;
  --resume-muted: #4b5b55;
  --resume-dark: #083127;
  --resume-card-bg: rgba(246, 255, 249, 0.92);
  --resume-border: rgba(63, 206, 123, 0.35);
  --resume-card-border: rgba(8, 49, 39, 0.08);
  --resume-card-shadow: 0 15px 32px -22px rgba(8, 49, 39, 0.45);
  --resume-hero-bg: radial-gradient(circle at top, rgba(63, 206, 123, 0.22), transparent 55%),
    linear-gradient(135deg, rgba(10, 138, 118, 0.25), rgba(63, 206, 123, 0.45));
  --resume-hero-shadow: 0 25px 50px -18px rgba(8, 49, 39, 0.35);
  --resume-pill-bg: rgba(63, 206, 123, 0.18);
  --resume-pill-border: rgba(8, 49, 39, 0.12);
  --resume-tag-bg: rgba(63, 206, 123, 0.18);
  --resume-tag-ghost-bg: rgba(8, 49, 39, 0.05);
  --resume-tag-ghost-border: rgba(8, 49, 39, 0.25);
  --resume-divider: rgba(8, 49, 39, 0.12);
  --resume-marker-glow: rgba(63, 206, 123, 0.25);
}

[data-theme='dark'] {
  --resume-muted: #9dcfb5;
  --resume-dark: #f4fff9;
  --resume-card-bg: rgba(5, 21, 18, 0.92);
  --resume-border: rgba(63, 206, 123, 0.55);
  --resume-card-border: rgba(63, 206, 123, 0.25);
  --resume-card-shadow: 0 20px 40px -22px rgba(0, 0, 0, 0.65);
  --resume-hero-bg: radial-gradient(circle at top, rgba(92, 255, 190, 0.15), transparent 55%),
    linear-gradient(135deg, rgba(5, 76, 63, 0.85), rgba(21, 131, 109, 0.65));
  --resume-hero-shadow: 0 30px 60px -25px rgba(0, 0, 0, 0.75);
  --resume-pill-bg: rgba(63, 206, 123, 0.22);
  --resume-pill-border: rgba(63, 206, 123, 0.4);
  --resume-tag-bg: rgba(63, 206, 123, 0.2);
  --resume-tag-ghost-bg: rgba(255, 255, 255, 0.06);
  --resume-tag-ghost-border: rgba(159, 255, 210, 0.3);
  --resume-divider: rgba(255, 255, 255, 0.08);
  --resume-marker-glow: rgba(63, 206, 123, 0.35);
}

.resume-page {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
  padding-bottom: 4rem;
}

.resume-hero {
  background: var(--resume-hero-bg);
  border: 1px solid var(--resume-border);
  border-radius: 1.5rem;
  padding: 2.75rem;
  display: flex;
  flex-wrap: wrap;
  gap: 2rem;
  align-items: center;
  box-shadow: var(--resume-hero-shadow);
}

.resume-hero__role {
  font-size: 1.5rem;
  font-weight: 500;
  color: var(--resume-muted);
  margin-left: 0.75rem;
}

.resume-hero__lead {
  margin-top: 1rem;
  font-size: 1.05rem;
  line-height: 1.8;
  max-width: 60ch;
}

.resume-hero__pills {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
}

.resume-pill {
  background: var(--resume-pill-bg);
  color: var(--resume-dark);
  border-radius: 999px;
  padding: 0.4rem 1rem;
  font-size: 0.95rem;
  border: 1px solid var(--resume-pill-border);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.resume-eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.12em;
  font-size: 0.8rem;
  color: var(--resume-muted);
  margin-bottom: 0.6rem;
}

.resume-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 1.5rem;
}

.resume-card,
.resume-section {
  background: var(--resume-card-bg);
  border-radius: 1.25rem;
  padding: 1.75rem;
  border: 1px solid var(--resume-card-border);
  box-shadow: var(--resume-card-shadow);
  backdrop-filter: blur(12px);
}

.resume-card h2,
.resume-section h2 {
  margin-top: 0;
  margin-bottom: 0.75rem;
}

.resume-skill-grid {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.resume-tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
}

.resume-tag {
  background: var(--resume-tag-bg);
  color: var(--resume-dark);
  border-radius: 0.7rem;
  padding: 0.2rem 0.75rem;
  font-size: 0.85rem;
}

.resume-tag--ghost {
  background: var(--resume-tag-ghost-bg);
  border: 1px dashed var(--resume-tag-ghost-border);
}

.resume-link-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 0.85rem;
}

.resume-link-list li {
  display: flex;
  flex-direction: column;
  gap: 0.1rem;
}

.resume-link-list span {
  font-size: 0.85rem;
  color: var(--resume-muted);
}

.resume-link-list a {
  color: var(--resume-dark);
  font-weight: 600;
  text-decoration: none;
}

.resume-section__header {
  margin-bottom: 1.75rem;
}

.resume-timeline {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.resume-timeline__item {
  border-left: 2px solid var(--resume-border);
  padding-left: 1.5rem;
  position: relative;
}

.resume-timeline__item::before {
  content: '';
  width: 12px;
  height: 12px;
  border-radius: 999px;
  background: var(--resume-accent);
  position: absolute;
  left: -7px;
  top: 0.4rem;
  box-shadow: 0 0 0 6px var(--resume-marker-glow);
}

.resume-role {
  font-weight: 600;
}

.resume-project {
  font-size: 0.9rem;
  color: var(--resume-muted);
}

.resume-project-grid {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
  margin-top: 1rem;
}

.resume-project-card {
  border: 1px dashed var(--resume-border);
  border-radius: 0.9rem;
  padding: 1rem 1.25rem;
  background: var(--resume-card-bg);
  box-shadow: inset 0 0 0 1px rgba(8, 49, 39, 0.02);
}

.resume-project-card ul {
  margin: 0.5rem 0 0.75rem;
  padding-left: 1.1rem;
}

.resume-project-card ul li {
  margin-bottom: 0.25rem;
}

.resume-project-card ul li:last-child {
  margin-bottom: 0;
}

.resume-project-card__header {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  align-items: baseline;
  margin-bottom: 0.45rem;
}

.resume-project-card__name {
  font-weight: 600;
}

.resume-project-card__period {
  font-size: 0.85rem;
  color: var(--resume-muted);
}

.resume-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.resume-list li {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  border-bottom: 1px dashed var(--resume-divider);
  padding-bottom: 0.75rem;
}

.resume-list li:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

@media (max-width: 768px) {
  .resume-hero {
    padding: 1.75rem;
  }

  .resume-hero__role {
    display: block;
    margin-left: 0;
    margin-top: 0.25rem;
  }

  .resume-timeline__item {
    padding-left: 1rem;
  }
}
`}</style>
